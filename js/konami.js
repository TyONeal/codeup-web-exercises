"use strict"

//make the inputs into an array


//write a function that takes in the correct order of inputs

const correctInput = ['ArrowUp', 'ArrowUp', 'ArrowDown', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'ArrowLeft', 'ArrowRight', 'b', 'a'];

//if function takes in correct inputs, do something
const submitting = document.querySelector('#submit-button')
const userTyping = document.querySelector('#user-input')
document.addEventListener('keyup', (e) => {

    });

